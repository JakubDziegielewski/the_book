use std::collections::HashMap;

fn main() {
    let ages: Vec<u8> = vec![1, 2, 3, 4, 5];
    let mut groups: Vec<Vec<u8>> = Vec::new();
    groups.push(vec![1,2]);

    groups[0].push(3);
    println!("{:?}, {:?}", ages, groups);
let mut a: HashMap<u8, &Vec<u8>> = HashMap::new();

}

fn my_sort(vector: Vec<u8>){
    
}